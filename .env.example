# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
APP_URL="MY_APP_URL"

# GEMINI_API_KEY: Powers brand analysis, content writing, Gemini images, and Veo video.
# Without it the app runs in demo mode (images still generate free via Pollinations).
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# KLING_ACCESS_KEY / KLING_SECRET_KEY: Enables Kling image-to-video generation
# (first choice in the video provider cascade). https://klingai.com
KLING_ACCESS_KEY=""
KLING_SECRET_KEY=""

# HIGGSFIELD_API_KEY: Enables Higgsfield cinematic video generation
# (second choice in the video provider cascade). https://cloud.higgsfield.ai
HIGGSFIELD_API_KEY=""

# CANVA_TEMPLATE_URL: Optional. A Canva template/design URL that the carousel
# editor's "Open in Canva" button opens instead of a blank design.
CANVA_TEMPLATE_URL=""

# GHL_API_TOKEN: GoHighLevel Private Integration Token — the PRIMARY publishing
# path. One token covers every social platform connected inside GHL; the app
# pushes posts into each brand's sub-account Social Planner and GHL publishes
# them at the scheduled time. Create it in GHL → Settings → Private
# Integrations with scopes: Social Media Posting (read/write) + Users (read).
GHL_API_TOKEN=""
# GHL_USER_ID: Optional. Pins the GHL user posts are created as; when unset
# the first user of the sub-account is used.
GHL_USER_ID=""

# META_APP_ID / META_APP_SECRET: Optional ALTERNATIVE to GHL — direct Meta
# (Instagram) connection for accounts not managed in GHL.
# Create a Business-type app at developers.facebook.com, add the Facebook Login
# + Instagram Graph API products, and whitelist {APP_URL}/api/meta/callback as
# an OAuth redirect URI. See README "Real Instagram publishing" for the full
# checklist. NOTE: publishing requires APP_URL to be a PUBLIC https origin —
# Meta's servers fetch your post images from {APP_URL}/media/:key.
META_APP_ID=""
META_APP_SECRET=""

# ANTHROPIC_API_KEY: Optional. Used by the /api/anthropic proxy route.
ANTHROPIC_API_KEY=""

# DB_PATH: SQLite file location. Defaults to ./data.db locally. On Render the
# render.yaml blueprint sets this to /data/data.db so the persistent disk
# holds it (brands, campaigns, posts, Meta tokens all survive redeploys).
DB_PATH=""

# APP_PASSWORD: password for the app's login gate. One shared password unlocks
# the workspace. REQUIRED in production — when unset, protected routes return
# 503 rather than serving an unauthenticated app. Unset locally, login is
# skipped for convenience.
APP_PASSWORD=""

# SESSION_SECRET: optional. HMAC key for session cookies. When unset, a random
# secret is generated once and stored in the database, so sessions survive
# restarts. Set it explicitly if you run more than one instance.
SESSION_SECRET=""
